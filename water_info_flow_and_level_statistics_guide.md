# 水文水質DBの流況・位況算定手順

この手順書では、国土交通省「水文水質データベース」のデータを使って、次の値を算定するまでの操作を説明します。

- 流量データから算定する流況
  - 豊水流量
  - 平水流量
  - 低水流量
  - 渇水流量
- 水位データから算定する位況
  - 豊水位
  - 平水位
  - 低水位
  - 渇水位

データの取得にはGUIを使用します。取得後の算定処理には、現在のところ専用GUIがないため、PowerShellから後処理モジュールを実行します。

```text
水文データ取得GUI
    ↓
時間データ・日データのExcel
    ↓
water_info.postprocess
    ↓
流況・位況の算定結果をExcel / Parquetへ出力
```

## 1. 対象データと入力ファイル

取得対象と後処理へ渡すファイルの対応は次のとおりです。

| 算定対象 | GUIの取得項目 | 時間データ | 日データ | 算定結果 |
|---|---|---|---|---|
| 流況 | 流量 | `_QH.xlsx` | `_QD.xlsx` | 豊水流量・平水流量・低水流量・渇水流量 |
| 位況 | 水位 | `_WH.xlsx` | `_WD.xlsx` | 豊水位・平水位・低水位・渇水位 |

時間データは必須です。日データは省略できますが、水文水質データベースの日データを使った結果も確認する場合は、時間データと同じ観測所・同じ期間で取得してください。

## 2. 実行環境を準備する

リポジトリのフォルダを共有された場合は、このファイルがあるフォルダをPowerShellで開き、以下のいずれかの方法で環境を準備します。

### 2.1 `uv`が使える場合

初回のみ依存関係を準備します。

```powershell
uv sync
```

アプリ選択画面を起動します。

```powershell
uv run python main.py
```

### 2.2 `uv`がない場合

Pythonがインストールされていれば、仮想環境を作成して実行できます。次のコマンドは初回のみ必要です。

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -e .
```

`python`コマンドが見つからず、Windowsの`py`ランチャーが使用できる場合は、1行目を次のように置き換えます。

```powershell
py -m venv .venv
```

準備後は、次のコマンドでアプリ選択画面を起動します。

```powershell
.\.venv\Scripts\python.exe main.py
```

### 2.3 配布用の実行ファイルがある場合

共有フォルダ内に配布用の`.exe`が含まれている場合は、Pythonや`uv`を準備せずに、その実行ファイルをダブルクリックしてGUIを起動できます。

ただし、現状の流況・位況算定処理はPowerShellから後処理モジュールを実行する必要があります。後処理まで行う場合は、`uv`またはPython仮想環境を準備してください。

## 3. GUIでデータを取得する

アプリ選択画面が開いたら、「国土交通省 水文データ取得」を選択します。

流況と位況で共通する操作は次のとおりです。

1. 対象の観測所コードを入力し、「追加」を押す。
2. 開始年月と終了年月を指定する。
3. 「指定全期間シート挿入」をオンにする。
4. 時間データと日データを別々に取得する。

複数年を処理する場合は、「指定全期間シート挿入」をオンにしてください。後処理はExcelの`全期間`シートを優先して読み込みます。

### 3.1 流況を算定するための流量データ

#### 時間流量を取得する

1. 取得項目で「流量」を選択する。
2. 「日データ」をオフにする。
3. 「実行」を押す。
4. 出力された`_QH.xlsx`を確認する。

#### 日流量を取得する

1. 同じ観測所コード・同じ取得期間を指定する。
2. 取得項目で「流量」を選択する。
3. 「日データ」をオンにする。
4. 「実行」を押す。
5. 出力された`_QD.xlsx`を確認する。

### 3.2 位況を算定するための水位データ

#### 時間水位を取得する

1. 取得項目で「水位」を選択する。
2. 「日データ」をオフにする。
3. 「実行」を押す。
4. 出力された`_WH.xlsx`を確認する。

#### 日水位を取得する

1. 同じ観測所コード・同じ取得期間を指定する。
2. 取得項目で「水位」を選択する。
3. 「日データ」をオンにする。
4. 「実行」を押す。
5. 出力された`_WD.xlsx`を確認する。

## 4. 後処理を実行する

後処理では、時間データを`--hour-file`、日データを`--daily-file`へ指定します。パスに日本語や空白が含まれる場合があるため、ファイルパスは`"`で囲んでください。

### 4.1 `uv`が使える場合

#### 流況を算定する

```powershell
uv run python -m water_info.postprocess `
  --hour-file "<時間流量ファイルのパス>_QH.xlsx" `
  --daily-file "<日流量ファイルのパス>_QD.xlsx" `
  --out-excel "outputs/flow_statistics/<観測所名>_流況算定.xlsx" `
  --out-parquet "outputs/flow_statistics/<観測所名>_parquet"
```

#### 位況を算定する

```powershell
uv run python -m water_info.postprocess `
  --hour-file "<時間水位ファイルのパス>_WH.xlsx" `
  --daily-file "<日水位ファイルのパス>_WD.xlsx" `
  --out-excel "outputs/level_statistics/<観測所名>_位況算定.xlsx" `
  --out-parquet "outputs/level_statistics/<観測所名>_parquet"
```

### 4.2 `uv`がない場合

「2.2 `uv`がない場合」で作成した仮想環境のPythonを使用します。

#### 流況を算定する

```powershell
.\.venv\Scripts\python.exe -m water_info.postprocess `
  --hour-file "<時間流量ファイルのパス>_QH.xlsx" `
  --daily-file "<日流量ファイルのパス>_QD.xlsx" `
  --out-excel "outputs/flow_statistics/<観測所名>_流況算定.xlsx" `
  --out-parquet "outputs/flow_statistics/<観測所名>_parquet"
```

#### 位況を算定する

```powershell
.\.venv\Scripts\python.exe -m water_info.postprocess `
  --hour-file "<時間水位ファイルのパス>_WH.xlsx" `
  --daily-file "<日水位ファイルのパス>_WD.xlsx" `
  --out-excel "outputs/level_statistics/<観測所名>_位況算定.xlsx" `
  --out-parquet "outputs/level_statistics/<観測所名>_parquet"
```

### 4.3 日データを使用しない場合

日データを取得していない場合は、`--daily-file`の行を削除します。時間データから作成した日平均値を使って算定されます。

`--out-excel`は必須です。Parquetが不要な場合は、`--out-parquet`の行を削除できます。

## 5. 算定方法

時間データは、1:00から翌日の0:00までを同じ水文日として日単位に集計します。年ごとに日値を大きい順に並べ、次の順位に対応する値を算定します。

| 基準順位 | 流況としての名称 | 位況としての名称 | 内部名 |
|---:|---|---|---|
| 95位 | 豊水流量 | 豊水位 | `ikyo_high` |
| 185位 | 平水流量 | 平水位 | `ikyo_normal` |
| 275位 | 低水流量 | 低水位 | `ikyo_low` |
| 355位 | 渇水流量 | 渇水位 | `ikyo_drought` |

1位がその年の最大値です。閏年や欠測数に応じて、標準版では採用順位が補正されます。

### 算定対象となる系列

| 系列 | 内容 |
|---|---|
| `var_den` | 欠測を除いた時間値で計算した日平均 |
| `fixed_den` | 24時間すべてそろっている日のみ計算した日平均 |
| `daily_value` | 水文水質データベースから取得した日データ |

`daily_value`は、`--daily-file`を指定した場合だけ出力されます。

標準版では、対象年の欠測が11件以上ある系列は算定不能として`NaN`になります。欠測閾値・順位補正を適用しない参考版も同時に出力されます。

## 6. 出力結果を確認する

### 6.1 Excel

既定では、次のシートが出力されます。

| シート | 内容 |
|---|---|
| `main` | 日次値、非欠損本数、年ごとのランク |
| `peaks` | 日別のピーク値と発生時刻 |
| `summary_adj` | 欠測閾値・順位補正を適用した標準版の年次サマリ |
| `summary_raw` | 欠測閾値・順位補正を適用しない参考版の年次サマリ |

通常は`summary_adj`を確認し、欠測の影響や補正前の値を比較したい場合に`summary_raw`を使用します。

### 6.2 流況結果を読むときの注意

現行の後処理は、Excelの項目名を水位向けの名称で出力します。そのため、流量ファイル（`_QH.xlsx` / `_QD.xlsx`）を入力した場合は、次のように読み替えます。

| Excelに表示される名称 | 流況としての読み方 |
|---|---|
| 位況豊水位 | 豊水流量 |
| 位況平水位 | 平水流量 |
| 位況低水位 | 低水流量 |
| 位況渇水位 | 渇水流量 |

水位ファイル（`_WH.xlsx` / `_WD.xlsx`）を入力した場合は、Excelの名称をそのまま位況として使用できます。

### 6.3 Parquet

`--out-parquet`を指定した場合は、主に次のファイルが出力されます。

| ファイル | 内容 |
|---|---|
| `df_hour_raw.parquet` | 読み込んだ時間データ |
| `df_hour_daily.parquet` | 時間データの日次集計 |
| `df_merged.parquet` | 日データとの結合結果と流況・位況の列 |
| `df_summary_peak.parquet` | 日別ピーク |

日データを指定しない場合は、`df_merged.parquet`は出力されません。

## 7. よくあるエラーと確認事項

### 時間データと日データを取り違えた

- `--hour-file`には`_QH.xlsx`または`_WH.xlsx`を指定します。
- `--daily-file`には`_QD.xlsx`または`_WD.xlsx`を指定します。

取り違えた場合は、1日あたりの行数を検査してエラーになります。

### 流量と水位を混在させた

同じ算定処理では、流量ファイル同士または水位ファイル同士を組み合わせてください。

- 正しい組み合わせ: `_QH.xlsx`と`_QD.xlsx`
- 正しい組み合わせ: `_WH.xlsx`と`_WD.xlsx`
- 誤った組み合わせ: `_QH.xlsx`と`_WD.xlsx`

### 複数年のうち一部しか処理されない

GUIで再取得し、「指定全期間シート挿入」をオンにしてください。後処理は`全期間`シートを優先して読み込みます。

### 流況の出力が水位表記になる

現行実装の制約です。入力ファイルが流量であれば、値は流量を使って計算されています。「6.2 流況結果を読むときの注意」の対応表で読み替えてください。

### 後処理用の画面が見つからない

現在、流況・位況算定専用のGUIはありません。データ取得はGUIで行い、算定処理だけPowerShellから実行します。

## 8. 現在の制約と今後の対象

- データ取得はGUIで行い、算定処理は後処理モジュールを実行する2段階構成です。
- 後処理には流況・位況を切り替える`--mode`がありません。入力したExcelが流量か水位かを利用者が管理します。
- Excelの日本語項目名は水位向けに固定されています。
- 今後、流況・位況の両方を明示的に扱えるGUIや、入力種別に応じた出力名称への対応を想定しています。

## 9. 関連ドキュメント

- [Water Info後処理の概要](docs/advanced/postprocess.md)
- [Water Info後処理の要件](docs/dev/requirements/postprocess.md)
- [Water Infoの構成](docs/dev/architecture/water_info.md)
