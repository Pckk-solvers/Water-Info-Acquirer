# スタイル表示モードの JSON 同期と配置変更

## 状態
取消

## 理由
- 1件の task としては対象範囲が広く、1回の実装・確認で閉じる粒度ではなかったため。
- 実装責務を `style JSON`、`UI 同期`、`preview 伝播`、`描画レンジ切替` に分割した。

## 元の内容
- 元のタスクは `docs/dev/tasks/active/` から分割後の 4 タスクへ引き継いだ。
- 既存の要件・設計の正本は維持し、作業計画だけを細分化した。
