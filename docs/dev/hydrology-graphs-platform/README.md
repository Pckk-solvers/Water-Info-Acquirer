# Hydrology Graphs Platform Docs

Status: current

このディレクトリは以下の3区分で管理する。

- `current/`: 現在実装されている仕様の正本
- `target/`: 次フェーズ計画（現時点は空）
- `archive/`: 廃止・差し替え済みの履歴

運用ルール:
- 実装変更時は `current/` を更新する。
- 実装前検討が発生したときのみ `target/` を作成・更新する。
- 旧版は `archive/` へ移す（編集しない）。
