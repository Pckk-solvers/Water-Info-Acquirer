# 現状仕様でExcel出力する際にもっと良い方法を検討するべく各シートで必要のないものを書き出していく

## main

### 方針
- 位況列は削除し、値＋ランクのみにする。
- 参考ランク（旧 main_raw_rank のランク列）をここにマージして持つ。
- ランクは欠損閾値で NaN になる標準版と、閾値なしの参考版を並置する。

## main_raw_rank
出力しない。参考ランクは main に統合する。

## peaks
現状維持

## year_summary
デフォルトシート名をよりわかりやすく変更（例: summary_adj）。
データ構造は現状維持

## year_summary_raw
デフォルトシート名をよりわかりやすく変更（例: summary_raw）。
データ構造は現状維持
