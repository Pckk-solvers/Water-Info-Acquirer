# ランチャー

起動すると「ランチャー」画面が表示され、次の2つのアプリをボタンで選択できます。

- **国交省 水文データ取得**
- **気象庁 雨量データ取得**

## 画面遷移

- どちらのアプリを開いても、上部の **「メニュー」** からもう一方へ移動できます。
- アプリのウィンドウを閉じると、ツール全体が終了します。

!!! tip "起動が重いとき"
    起動直後は依存ライブラリの読み込みが走るため、ボタンが一時的に無効化されます。表示が「準備完了」になってから選択してください。

